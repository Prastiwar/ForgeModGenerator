<Page x:Class="ForgeModGenerator.ViewModel.TextureGeneratorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ForgeModGenerator.ViewModel"
      xmlns:control="clr-namespace:ForgeModGenerator.Controls"
	  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
	  local:ViewModelLocator.GetDataContext="ForgeModGenerator.ViewModel.TextureGeneratorViewModel">

	<Grid Background="#FF424242">

		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<StackPanel>
				<materialDesign:Card Background="{DynamicResource MaterialDesignBackground}" Margin="10">
					<StackPanel>
						<control:ImageList HeaderText="Blocks" Images="{Binding Blocks}" AddCommand="{Binding AddTexture}"/>
						<Border Background="{DynamicResource MaterialDesignDivider}" Height="1" HorizontalAlignment="Stretch" SnapsToDevicePixels="True" />

						<control:ImageList HeaderText="Entity" Images="{Binding Entity}" AddCommand="{Binding AddTexture}"/>
						<Border Background="{DynamicResource MaterialDesignDivider}" Height="1" HorizontalAlignment="Stretch" SnapsToDevicePixels="True" />

						<control:ImageList HeaderText="Items" Images="{Binding Items}" AddCommand="{Binding AddTexture}"/>
						<Border Background="{DynamicResource MaterialDesignDivider}" Height="1" HorizontalAlignment="Stretch" SnapsToDevicePixels="True" />

						<control:ImageList HeaderText="Armor" Images="{Binding Armor}" AddCommand="{Binding AddTexture}"/>
						<Border Background="{DynamicResource MaterialDesignDivider}" Height="1" HorizontalAlignment="Stretch" SnapsToDevicePixels="True" />
					</StackPanel>
				</materialDesign:Card>
			</StackPanel>
		</ScrollViewer>

	</Grid>
</Page>
