<Page x:Class="ForgeModGenerator.ViewModel.TextureGeneratorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ForgeModGenerator.ViewModel"
      xmlns:control="clr-namespace:ForgeModGenerator.Controls"
	  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" x:Name="TexturePageWindow"
	  local:ViewModelLocator.GetDataContext="ForgeModGenerator.ViewModel.TextureGeneratorViewModel">

	<Grid Background="#FF424242">
		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<StackPanel>
				<materialDesign:Card Background="#FF535353" Margin="10">
					<StackPanel>
						<ListBox ItemsSource="{Binding Textures}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
							<ListBox.ItemTemplate>
								<DataTemplate>
									<StackPanel>
										<control:TextureList HeaderText="{Binding Path=HeaderName}" Textures="{Binding}"
															AddCommand="{Binding Path=DataContext.AddTexture, RelativeSource={RelativeSource AncestorType=ListBox}}"
															 RemoveCommand="{Binding Path=DataContext.RemoveTexture, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
										<Border Background="#FF313131" Height="1" HorizontalAlignment="Stretch" SnapsToDevicePixels="True" />
									</StackPanel>
								</DataTemplate>
							</ListBox.ItemTemplate>
						</ListBox>
					</StackPanel>
				</materialDesign:Card>
			</StackPanel>
		</ScrollViewer>

	</Grid>
</Page>
